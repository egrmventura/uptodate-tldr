Write a short STATUS.md summarizing the session for context restoration. Goal: at the end of a work session, generate or overwrite `STATUS.md` at the repo root capturing — current branch and last commit, what was completed this session, what is in progress or pending (with the next concrete step), any known issues or blockers, and the recommended next command/goal to run. Key constraint: keep it short and skimmable (a page or less), state facts verifiable from git and the code (no speculation), convert relative dates to absolute, and mirror the module vocabulary used in `ARCHITECTURE.md` and `CLAUDE.md` so a fresh session can restore context in one read. Validate by confirming `STATUS.md` exists, names the correct current branch and HEAD commit, and lists at least one pending item with an actionable next step.
