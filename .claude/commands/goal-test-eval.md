Strengthen the test and evaluation coverage. Goal: two parts — (1) fix the four pre-existing pytest fixture errors (`test_config_topic`, `test_hn_source`, `test_arxiv_source`, `test_e2e_ingest_and_rank` expect undefined `topic`/`config` fixtures) so the whole suite runs green under `pytest`, and (2) add a lightweight LLM-output eval harness that scores grouper and analyst responses against a small labeled fixture set (do topics group into the expected canonical labels? does analysis JSON parse and populate all four fields?). Key constraint: the eval harness runs offline against recorded LLM responses (no live API in CI), and the fixture fixes must not weaken existing assertions. Validate by running `python3 -m pytest test_pipeline.py -v` and confirming zero errors and zero failures, plus an eval run that reports pass/fail counts per module against the labeled set.
