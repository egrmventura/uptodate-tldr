Run an error-handling and resilience pass across the pipeline. Goal: systematically audit every external-I/O and LLM boundary (sources, scraper, Anthropic calls in grouper/analyst/summarizer, SQLite writes, delivery channels) and confirm each has explicit, tested failure handling — retries with backoff where transient (arXiv 429, API rate limits), log-and-skip where isolated, and abort only where there is genuinely nothing to deliver. Key constraint: no bare `except: pass`, every swallowed exception is logged with context, and the resilience rules match what CLAUDE.md documents (source failure isolated, summarization/analysis failure raises, channel failure isolated unless all fail). Validate by injecting faults — a timing-out source, a 429, a malformed LLM JSON response, a locked SQLite db — and asserting each is handled per its documented contract without crashing the run or corrupting persisted state.
