Harden the pipeline glue connecting the stages. Goal: audit and tighten the seams between ingest → (scrape) → group → analyze → persist → deliver so each stage has a single, explicit hand-off contract (typed inputs/outputs, no reaching across stages), and the orchestrator in `analyze.py` reads as a linear stage sequence rather than inlined logic. Key constraint: preserve current behavior exactly — `main.py`'s TLDR flow stays untouched, stage boundaries keep their existing failure semantics (source/enrichment isolated, summarization/analysis abort-or-skip as today), and no stage silently swallows a downstream contract violation. Validate with a stage-boundary test that feeds a known item set through the full glue with mocked LLM calls, asserting each stage receives and returns the expected type and that an empty hand-off (e.g. zero groups) short-circuits cleanly without reaching delivery.
